# Backend environment variables
# Copy to .env before running docker compose if you need to override defaults.
# Example:
# APP_ENV=production

# Admin interface credentials (used by POST /admin/login)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me

# Secret used to sign admin session tokens (header: X-Admin-Token)
ADMIN_API_KEY=change-me-too

# Optional: override the lifetime (in seconds) of admin session tokens
# ADMIN_TOKEN_TTL=3600
